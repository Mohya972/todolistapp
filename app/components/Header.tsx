import React from 'react'
import { Text } from 'react-native'
import { styles } from '../(tabs)/styles'

const Header = () => {
    return (
        <Text style={styles.title}>Ma TodoList</Text>
    )
}

export default Header